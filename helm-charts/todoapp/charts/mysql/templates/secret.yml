apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}-secrets
  namespace: {{ .Values.mysql.namespace }}
type: Opaque
data:
  MYSQL_ROOT_PASSWORD: {{ .Values.mysql.secrets.MYSQL_ROOT_PASSWORD | quote | b64enc }}
  MYSQL_USER: {{ .Values.mysql.secrets.MYSQL_USER | b64enc | quote }}
  MYSQL_PASSWORD: {{ .Values.mysql.secrets.MYSQL_PASSWORD | quote | b64enc }}
