todoapp:
  environment: staging
  namespace: todoapp-staging
  createNamespace: true

  service:
    type: NodePort
    ports:
      - name: http
        port: 80
        targetPort: 8080
        nodePort: 30008
        protocol: TCP

mysql:
  createNamespace: true
  enabled: true
  namespace: mysql-staging
  environment: staging
